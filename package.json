{
    "name": "anonymarket",
    "scripts": {
        "test": "cd contracts && npx hardhat test",
        "compile": "cd contracts && npx hardhat compile",
        "node": "cd contracts && npx hardhat node --watch",
        "frontend": "cd src && npm run start",
        "install": "cd contracts && yarn install && cd .. && cd src && yarn install && cd .."
    }
}
